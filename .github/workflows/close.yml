name: Close

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  close:
    name: Close
    runs-on: ubuntu-latest

    steps:
      - name: Closing
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
